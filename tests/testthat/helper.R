# Auto-sourced by testthat::test_dir() before running any test file.
# test_dir() runs with the working directory set to tests/testthat itself,
# not the project root, hence the "../../R" relative path.
purrr::walk(list.files("../../R", pattern = "\\.R$", full.names = TRUE), source)
